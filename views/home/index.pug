extends ../layout.pug

block head
	title #{pageTitle} | #{action}

block header
	if isAuthenticated
		include ../includes/header.pug
	else
		include ../includes/header-anon.pug

block content
	.container
		h1 Hello
		p You are #{isAuthenticated}